[phases.setup]
nixPkgs = ["ffmpeg", "python311", "nodejs_18"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd frontend && npm install && npm run build"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
